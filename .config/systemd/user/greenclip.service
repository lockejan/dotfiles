[Unit]
Description=Start greenclip clipboard manager daemon
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/greenclip daemon

[Install]
WantedBy=graphical-session.target
